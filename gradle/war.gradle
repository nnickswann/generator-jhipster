apply plugin: "war"

bootWar {
    mainClass = "com.mycompany.myapp.ForInterviewLigaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
